# Hebrew Vocab API

FastAPI backend for searching and serving a structured Hebrew vocabulary dataset.

## Overview

This project loads a pre-built Hebrew vocabulary dataset (`vocab_dataset.json`) into a database and exposes it through a REST API for fast search and lookup.
